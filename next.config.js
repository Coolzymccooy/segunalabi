/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The app directory is enabled by default in Next.js 14, so the
  // experimental.appDir flag has been removed.
};

module.exports = nextConfig;
